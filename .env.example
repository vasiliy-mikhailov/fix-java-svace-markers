# Local LLM (Qwen via vLLM) — the reasoning engine
QWEN_BASE_URL=http://inference-vllm:8000/v1
QWEN_API_KEY=sk-REPLACE_ME
QWEN_MODEL=qwen-3.6-27b-fp8

# GitHub token — used for shallow clones + repo metadata (read-only scope is enough)
GITHUB_TOKEN=ghp_REPLACE_ME

# tuning knobs
MAX_FILES=
PROVE_TOP=
THINK_BUDGET=
MAX=
