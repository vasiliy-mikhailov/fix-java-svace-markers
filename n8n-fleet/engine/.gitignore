# Maven build output, including target/pit-reports. The repo root has no `target` rule because
# nothing here was a Maven project before the engine.
target/

# Differential-harness output: the generated cases and both sides' results run to tens of megabytes
# and are reproduced by `sh harness/<slice>-run.sh` in seconds. The *-report.txt summaries are the
# evidence for a port and ARE committed.
harness/out/
