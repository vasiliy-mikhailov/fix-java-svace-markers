{
  "name": "fsm-agentic",
  "version": "1.0.0",
  "private": true,
  "description": "Generators for the fix-java-svace-markers n8n pipeline. Node bodies live in src/nodes as real modules so they can be tested and mutated with standard tooling.",
  "type": "commonjs",
  "scripts": {
    "gen": "node src/gen-setup.js && node src/gen-ingest.js && node src/gen-prover.js",
    "test": "node --test",
    "coverage": "c8 --reporter=text --reporter=json-summary --include 'src/**' node --test",
    "mutation": "stryker run"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^8.7.1",
    "c8": "^10.1.3"
  }
}