=\s*$|\)\s*\.\s*\w+\s*\(|\b[a-z][A-Za-z0-9_$]*\s*\.\s*[a-z][A-Za-z0-9_$]*\s*\(
DEREF_AFTER_NULL asserts that a reference the program itself has already admitted can be null — an earlier `x == null` branch, an `x = null` assignment, or a call whose own body has a `return null` path — is then dereferenced (`x.foo()`, `x.field`, `x[i]`) on a path where no guard dominates the use. The report anchors on the dereference site, not on the null-producing site, and because this tree is google-java-format-wrapped the anchored line is the FIRST line of the statement: at LessonTemplateResolver.java:61 the flagged line is the bare `var resource =`, and the actual chain `resourceLoader.getResource("classpath:/" + templateName).getInputStream().readAllBytes()` sits on line 62. Two neighbours are routinely mistaken for it: NULL_AFTER_DEREF is the reverse order (dereference first, redundant `!= null` check after — a readability finding that no test can turn into a failure), and DEREF_OF_NULL is the unconditional case where the reference is null on every path, so its NPE needs no path construction; DEREF_AFTER_NULL is path-conditional, which is precisely why demonstrating it means building the path.

HOW YOU MAKE IT VISIBLE.
Every DEREF_AFTER_NULL has two sites; the marker gives you one. Before writing a line of test, name both: D, the dereference on the flagged statement (enumerate them — line 62 has three: `resourceLoader`, the `Resource` returned by `getResource`, the `InputStream` returned by `getInputStream`), and N, the program fact that says that reference can be null. The test is a route from N to D.

N must be something the PROGRAM can produce, not something a stub can be told to produce. Real sources in this repo: a same-file `return null` branch (LessonTemplateResolver.java:69; EncDec.java:32-34, whose null return is pinned by EncDecTest.testNullDecode); a `Map.get` miss (LessonTemplateResolver.java:48); a repository lookup that returns null on empty (MissingAccessControlUserRepository.findByUsername, line ~38); `HttpServletRequest.getCookies()` (null when the request carries none), `getHeader`, `getParameter`, `getSession(false)`, `session.getAttribute(...)` (checked correctly at AsciiDoctorTemplateResolver.determineLanguage, `if (null != browserLocale)`); `Optional.orElse(null)`; `System.getenv`. If no such source feeds D, there is no defect to show — see the last paragraph.

Mechanics. These classes are plain objects, not Spring-only beans: `new LessonTemplateResolver(resourceLoader)` works standalone, and `computeTemplateResource` is `protected`, so a test placed in package `org.owasp.webgoat.container` (the source root `src/test/java/org/owasp/webgoat/container/` already exists) can call it directly — no context, no MockMvc. spring-boot-starter-test brings JUnit 5, AssertJ and Mockito, all used throughout `src/test/java`.

RED shape: arrange the precondition of N, call the smallest unit containing D, and assert the NPE *by which dereference blew up*, not merely that some NPE escaped — JDK 14+ helpful messages make this checkable: `assertThatThrownBy(() -> resolver.computeTemplateResource(null, null, null, "lesson:Missing.html", "UTF-8", Map.of())).isInstanceOf(NullPointerException.class).hasMessageContaining("is null")` with the message naming the exact expression. Without that, an NPE thrown by your own arrangement (pass a null `resourceName` and line 47's `resourceName.substring(...)` throws first) counts as a pass and proves nothing. GREEN shape: assert the post-fix BEHAVIOUR, not the absence of an exception — for this class, that the call returns the fallback `StringTemplateResource` whose text (read back via `ITemplateResource#reader()`) is "Unable to find lesson HTML: ...". "No exception thrown" also passes when the test stopped reaching the code at all.

For this particular marker the route is short and it ends in a refutation you can execute rather than argue. The only in-program null in the file is `loadAndCache`'s `return null` at line 69, and it is guarded twice before any use — line 49 and again at line 53, which returns the fallback before line 56 dereferences. Drive it for real: `new LessonTemplateResolver(new DefaultResourceLoader())` and `computeTemplateResource(..., "lesson:DoesNotExist.html", ...)`; `getResource("classpath:/DoesNotExist.html")` returns a non-null ClassPathResource whose `getInputStream()` throws FileNotFoundException, so line 69 fires, and the assertion that the fallback resource comes back proves the null path is both reachable and handled. Nothing on line 61-62 itself can be null in-program: Spring's `ResourceLoader.getResource` returns a handle unconditionally (a handle does not imply an existing resource), and `getInputStream()` signals failure by throwing IOException, never by returning null.

When the only way to get a null into D is a stub that contradicts the API contract, say so and settle the marker as refuted — cite the guard and the contract. Do not "fix" it by adding a defensive `if (... == null)` on a path no input can reach: a dead guard is uncoverable, and it makes the marker unfalsifiable in the other direction instead of settling it.

CHECKED AGAINST THE CHECKOUT BY A SECOND READER WHO RE-RAN THE CLAIMS ABOVE. Where the two disagree, THIS IS THE ONE THAT HOLDS.
Every DEREF_AFTER_NULL has two sites; the marker gives you one. Before writing a line of test, name both: D, the dereference on the flagged statement (enumerate them — line 62 has three: `resourceLoader`, the `Resource` returned by `getResource`, the `InputStream` returned by `getInputStream`), and N, the program fact that says that reference can be null. The test is a route from N to D.

N must be something the PROGRAM can produce on some real input, not something a stub can be told to produce and not something you hand in yourself through a constructor or parameter that no caller passes null to. Real sources in this repo: a same-file `return null` branch (LessonTemplateResolver.java:69; EncDec.java:32-34, whose null return is pinned by EncDecTest.testNullDecode); a `Map.get` miss (LessonTemplateResolver.java:48); a repository lookup that returns null on empty (MissingAccessControlUserRepository.findByUsername, line 37-39); `HttpServletRequest.getCookies()` (null when the request carries none), `getHeader`, `getParameter`, `getSession(false)`, `session.getAttribute(...)` (checked correctly at AsciiDoctorTemplateResolver.determineLanguage:126-135, `if (null != browserLocale)`); `Optional.orElse(null)`; `System.getenv`. Finding such a source is not enough — you must also show it REACHES D. Both nulls in this file are produced and then guarded upstream, which is the whole verdict here.

Mechanics. These classes are plain objects, not Spring-only beans: `new LessonTemplateResolver(resourceLoader)` works standalone, and `computeTemplateResource` is `protected`, so a test placed in package `org.owasp.webgoat.container` (the source root `src/test/java/org/owasp/webgoat/container/` already exists) can call it directly — no context, no MockMvc. spring-boot-starter-test brings JUnit 5, AssertJ and Mockito, all used throughout `src/test/java`.

RED shape, when a marker of this family IS provable: arrange the precondition of N, call the smallest unit containing D, and assert the NPE *by which dereference blew up*, not merely that some NPE escaped — JDK 14+ helpful messages make this checkable, e.g. `.hasMessageContaining("Cannot invoke \"X.y()\" because \"<expr>\" is null")` naming the exact expression from the flagged line. Without that pin, an NPE thrown by your own arrangement counts as a pass and proves nothing. GREEN shape: assert the post-fix BEHAVIOUR, not the absence of an exception — for a resolver, that the call returns the fallback `StringTemplateResource` whose text (read back via `ITemplateResource#reader()`) is the expected message. "No exception thrown" also passes when the test stopped reaching the code at all.

THIS MARKER IS REFUTED, and the refutation is executable rather than arguable — do not spend an attempt trying to make it RED, because no arrangement of real inputs can:
- The only in-program null in the file is `loadAndCache`'s `return null` at line 69, and it is guarded twice before any use: line 49, and again at line 53, which returns the fallback before line 56 dereferences.
- Nothing on line 61-62 can be null in-program. Spring's `ResourceLoader.getResource` is documented "never null" and returns a handle unconditionally (a handle does not imply an existing resource); `getInputStream()` signals failure by throwing IOException, never by returning null. Verified by running it: `new DefaultResourceLoader().getResource("classpath:/DoesNotExist.html")` returns `class path resource [DoesNotExist.html]`, and its `getInputStream()` throws `java.io.FileNotFoundException`.
- The drive to write, which passes on today's code and documents the reachable-and-handled null path: `new LessonTemplateResolver(new DefaultResourceLoader())`, then `computeTemplateResource(null, null, null, "lesson:DoesNotExist.html", "UTF-8", Map.of())`, then assert the returned `ITemplateResource` is a `StringTemplateResource` whose `reader()` yields exactly `Unable to find lesson HTML: DoesNotExist.html`. I ran this: line 69 fires (the `log.error` appears) and the fallback comes back. Note that this is a coverage/refutation drive, NOT a RED — it passes before any change, and that is the point.

Two ways to fake a RED here; both are false proofs, and both must be named in the settlement rather than committed:
- `new LessonTemplateResolver(null)` really does throw at line 62 with a perfectly pinned message (`Cannot invoke "org.springframework.core.io.ResourceLoader.getResource(String)" because "this.resourceLoader" is null`). It satisfies the message-pinning rule and uses no mock, so it looks legitimate — but the only construction site is `MvcConfiguration.java:114`, `new LessonTemplateResolver(resourceLoader)`, whose argument is the Spring-injected `ResourceLoader` bean parameter of `lessonTemplateResolver(...)` and is never null. Passing null yourself is a caller-contract violation, not the defect the checker claims.
- Stubbing `getResource`/`getInputStream` to return null (see trap 1).

Settle it as refuted, citing the two guards (lines 49 and 53) and the two API contracts. Do not "fix" it by adding a defensive `if (... == null)` on line 62's result: a dead guard is uncoverable, and it makes the marker unfalsifiable in the other direction instead of settling it.

WHAT GOES WRONG HERE.
1) Mockito-ing the null into existence: `when(resourceLoader.getResource(any())).thenReturn(null)` and calling the resulting NPE on line 62 a RED. Spring's `ResourceLoader`/`Resource` never hand back null — `getInputStream()` throws IOException instead — so that test proves the mock lied, not that the code is wrong. It is the same failure as binding DM_DEFAULT_ENCODING to `Charset.defaultCharset()`: it tests the shape of the checker's name.

2) `assertThrows(NullPointerException.class, ...)` with no message assertion. `computeTemplateResource` takes six parameters that are easy to pass as null, and `resourceName.substring(PREFIX.length())` at line 47 NPEs before control ever reaches 61. A green RED that fired four lines early is a false proof; pin the helpful-NPE text to the intended dereference.

3) Reading line 61 literally, finding `var resource =`, and concluding the marker points at nothing (or writing the note from the checker name instead). The anchor is the first line of a wrapped statement; the construct is lines 61-62 together.

4) Assuming an NPE is observable as a thrown exception. SpoofCookieAssignment.cookieLoginFlow:87-94 is this repo's textbook instance — `EncDec.decode(cookieValue).toLowerCase()` at line 90 dereferences a value whose producer returns null for null input — but the whole thing sits inside `catch (Exception e) { return failed(this).output(e.getMessage()) }`, so a real null yields a 200 with the NPE text in the body. A test asserting a thrown exception fails there even though the defect occurred; assert on the AttackResult / response body.

5) Deciding the verdict from the directory. "It is WebGoat, so it is intentional" does not follow: the lessons teach SQL injection, weak crypto, path traversal and missing access control — no assignment's success condition is a NullPointerException, and none is solved by crashing the handler. The opposite shortcut is equally wrong: container code is not the only place a defect can live, and `src/main/java/org/owasp/webgoat/lessons/**` plus `src/test/java/**` contain ordinary defects. What actually constrains a fix in lesson code is the specific assignment's success path, which its own test under `src/test/java/org/owasp/webgoat/lessons/**` already encodes — read that before changing control flow, because an early `return failed(...)` can change the result a correct solution produces.
